service:
  pipelines:
    metrics:
      receivers: [prometheus]
      processors: [memory_limiter, filter, k8sattributes, batch]
      exporters: [prometheusremotewrite/elasticsearch, prometheusremotewrite/mimir]

  extensions:
    - health_check
