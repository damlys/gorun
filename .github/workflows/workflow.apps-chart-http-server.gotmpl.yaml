name: apps-chart-http-server
on:
  pull_request:
    paths:
      - "projects/apps/helm-charts/http-server/**"
      - "!projects/apps/helm-charts/http-server/README.md"
  push:
    branches:
      - main
    paths:
      - "projects/apps/helm-charts/http-server/**"
      - "!projects/apps/helm-charts/http-server/README.md"
jobs:
  apps-chart-http-server:
    name: apps-chart-http-server
    runs-on: ubuntu-26.04
    container: damlys/gorun-devcontainer:0.9.100
    permissions:
      contents: write
      id-token: write
    env:
      DOCKER_BUILDKIT: 1
      HELM_EXPERIMENTAL_OCI: 1
      TF_IN_AUTOMATION: 1
    steps:
      - name: git clone (PR)
        if: github.event_name == 'pull_request'
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - name: git clone (not PR)
        if: github.event_name != 'pull_request'
        uses: actions/checkout@v4
      - name: git clone (fix)
        run: git config --system --add safe.directory "$GITHUB_WORKSPACE"
      - name: git config user
        run: git config --global user.name "github-actions" && git config --global user.email "github-actions@github.com"

      - name: gcloud auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GOOGLE_CREDENTIALS }}
          create_credentials_file: true
          cleanup_credentials: true
      - name: helm auth (Google Artifact Registry)
        run: gcloud auth print-access-token | helm registry login --username="oauth2accesstoken" --password-stdin "europe-central2-docker.pkg.dev"

      - name: build
        run: ./scripts/helm-chart build "projects/apps/helm-charts/http-server"
      - name: test
        run: ./scripts/helm-chart test "projects/apps/helm-charts/http-server"
      - name: pre-publish
        if: github.event_name == 'pull_request' && github.base_ref == 'main'
        run: ./scripts/helm-chart pre-publish "projects/apps/helm-charts/http-server"
      - name: publish
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: ./scripts/helm-chart publish "projects/apps/helm-charts/http-server"
      - name: show
        run: ./scripts/helm-chart show "projects/apps/helm-charts/http-server"
