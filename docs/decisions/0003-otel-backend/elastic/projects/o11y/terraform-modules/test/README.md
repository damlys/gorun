This module deploys observability tools to the test platform.

```
$ terraform apply -target=helm_release.opentelemetry_operator
$ terraform apply -target=helm_release.elastic_operator
$ terraform apply
```
